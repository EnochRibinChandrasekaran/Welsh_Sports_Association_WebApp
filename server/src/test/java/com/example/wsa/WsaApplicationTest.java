package com.example.wsa;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test class for WsaApplication to ensure that the application context loads successfully.
 */
@SpringBootTest
public class WsaApplicationTest {

  /**
   * Test method to check if the application context loads without errors.
   */
  @Test
  void contextLoads() {
    // The test will fail if the application context cannot start
  }
}
